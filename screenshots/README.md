# Screenshots

Drop the following PNG/JPG files here (exact filenames — the main README references them):

| Filename | What to capture |
| --- | --- |
| `landing.png` | The homepage hero — wordmark + the AirPods "Resell" vs running-shoe "Donate" contrast cards. The single most important shot. |
| `decision-math.png` | Intake **Step 3** — the value-vs-cost math: `expectedResaleValue − processingCost = netResell` and the routing call. This is the differentiator. |
| `condition-report.png` | The AI condition report — grade, observations, and confidence bar from the intake flow. |
| `marketplace.png` | The Encore **Campus** marketplace tab with verified listings. |
| `dashboard.png` | The impact dashboard — value recovered, kg CO₂ saved, decision breakdown. |

Recommended width ~1400px, compressed (each ideally < 400 KB) so the README loads fast.
